{
  "name": "@design-port/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for exposing DesignPort staged selections to Claude",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "design-port-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.10.0"
  }
}
